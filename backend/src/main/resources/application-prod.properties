# Production profile: keep SQL logging off and allow schema evolution for current release.
spring.jpa.show-sql=false
# Avoid startup failure when new entities are deployed against an existing DB.
spring.jpa.hibernate.ddl-auto=update
